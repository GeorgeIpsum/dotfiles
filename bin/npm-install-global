#!/bin/sh

if test ! $(which "$1")
then
  if test $(which npm)
  then
    echo "Installing ${1}"
    npm install -g "$1";
  else
    echo "npm is not installed!";
  fi
else
  echo "${1} is already installed.";
fi